name: Auto PR

on: pull_request

jobs:
    build:
        name: Auto Pull Request
        runs-on: ubuntu-latest
        permissions:
            pull-requests: write
        steps:
            - uses: hmarr/auto-approve-action@v4
